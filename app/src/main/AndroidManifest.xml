<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.outven.bmtchallange">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_aplikasi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_aplikasi_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BrushMyTeethChallange"
        android:fullBackupContent="@xml/backup_descriptor">
        <meta-data android:name="io.sentry.dsn" android:value="https://1c7ad4e0d2e445b599768e3c53d0d8b1@app.glitchtip.com/633" />
        <activity android:name=".activities.PrologActivity"/>
        <activity android:name=".activities.AchivmentActivity" />
        <activity android:name=".activities.EditProfileActivity" />
        <activity android:name=".activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AnimasiActivity" />
        <activity android:name=".activities.ProfilActivity" />
        <activity android:name=".activities.LupaPasswordActivity" />
        <activity android:name=".activities.UploadAfterDoneActivity" />
        <activity android:name=".activities.UploadAfterActivity" />
        <activity android:name=".activities.UploadBeforeDoneActivity" />
        <activity android:name=".activities.MulaiActivity" />
        <activity
            android:name=".activities.FullScreenActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen" />
        <activity
            android:name=".activities.LoginActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".activities.RegisterActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".activities.DashboardActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".activities.UploadBeforeActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>